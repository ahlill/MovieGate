<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="id.tpusk.moviegate">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo_video_player"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo_video_player"
        android:supportsRtl="true"
        android:theme="@style/Theme.MovieGate">
        <activity android:name=".ui.home.HomeActivity" />
        <activity android:name=".ui.detail.DetailFilmActivity" />
        <activity android:name=".ui.splashscreen.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>